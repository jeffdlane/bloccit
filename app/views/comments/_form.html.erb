<%= form_for [topic, post, comment], remote: true, html: { class: 'form-horizontal'} do |f| %>
  <%#= form_for [comment.post.topic, comment.post, comment], html: { class: 'form-horizontal'} do |f| %>
  <!-- <form action='/topics/1/posts/2/comments'>
  POST /topics/1/posts/2/comments -->

  <%= render partial: 'shared/errors', locals: { object: comment } %>

    <%= control_group_tag(comment.errors[:body]) do %> 
      <%= f.label :body, "Add Comment", class: 'control-label' %>
      <div class="controls">
        <%= f.text_field :body, rows: 8 %>
      </div>
    <% end %>

    <div class="control-group">
      <div class="controls">
        <%= f.submit "Save", class: 'btn' %>
      </div>
    </div>
<% end %>


<!-- @user.posts.each do |post|
  = post.body
  = render partial: 'comments/form', locals: {comment: post.comments.new} -->