<%= form_for comment, html: { class: 'form-horizontal'} do |f| %>
    <%= f.label :body, class: 'control-label' %>
    <%= f.text_field :body, class: 'control-label' %>
    <%= f.submit "Save", class: 'btn' %>
<% end %>